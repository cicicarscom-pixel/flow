const fs = require("fs"); let c = fs.readFileSync("src/modules/randevu/domain/entities/Appointment.ts", "utf8"); c = c.replace(/private readonly _bookingToken: string;/g, "private readonly _bookingToken: string;\n  private readonly _calendarId: string | null;"); c = c.replace(/bookingToken: string;/g, "bookingToken: string;\n    calendarId?: string | null;"); c = c.replace(/this\._bookingToken = data\.bookingToken;/g, "this._bookingToken = data.bookingToken;\n    this._calendarId = data.calendarId || null;"); c = c.replace(/get bookingToken\(\): string { return this\._bookingToken; }/g, "get bookingToken(): string { return this._bookingToken; }\n  get calendarId(): string | null { return this._calendarId; }"); fs.writeFileSync("src/modules/randevu/domain/entities/Appointment.ts", c, "utf8"); console.log("OK");
