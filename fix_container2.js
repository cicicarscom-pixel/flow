const fs = require("fs"); let c = fs.readFileSync("src/core/container.ts", "utf8"); c = c.replace(/if \\(cls === \x27AppointmentRepository\x27\\) return appointmentRepository;/g, "if (cls === \x27AppointmentRepository\x27) return appointmentRepository;\n    if (cls === \x27CalendarRepository\x27) return calendarRepository;"); fs.writeFileSync("src/core/container.ts", c, "utf8"); console.log("OK");
